# Cloud-Sync: Postgres-Verbindung (Neon, Supabase, Vercel Postgres, ...)
# Ohne diese Variable laeuft die App lokal weiter, synchronisiert aber nicht.
DATABASE_URL="postgres://user:password@host/dbname?sslmode=require"
